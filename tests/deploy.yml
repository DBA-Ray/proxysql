---
- hosts: "{{ deploy_hosts }}"
  gather_facts: True
  remote_user: root
  vars:
    proxysql: False # Whether to install proxysql
    whether_uninstall: False # Whether to uninstall the original service and then install it
  roles:
    - dba_ray.proxysql
