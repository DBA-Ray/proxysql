---
- hosts: "{{ deploy_hosts }}"
  gather_facts: True
  remote_user: root
  vars:
    whether_uninstall: True # Whether to uninstall the original service and then install it
  roles:
    - dba_ray.proxysql
